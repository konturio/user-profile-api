server:
  port: 8631
  servlet:
    context-path: /userprofile

spring:
  liquibase:
    changelog: classpath:changelog/changelog.yaml
  datasource:
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: 'jdbc:tc:postgis:16-3.4-alpine:///user-profile-api?TC_INITSCRIPT=db/init_test_db.sql'
#    type: com.zaxxer.hikari.HikariDataSource
#    url: jdbc:postgresql://localhost:5432/userprofile
    password: user-profile-api
    username: user-profile-api
    hikari:
      maximum-pool-size: 10
      minimum-idle: 3
      auto-commit: false
      driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: none #none so that Hibernate doesn't update schema (this is already done by liquibase)
      use-new-id-generator-mappings: false #false means use postgres sequences
      dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
    open-in-view: false
  profiles:
    active: jwtAuthDisabled

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus

springdoc:
  writer-with-default-pretty-printer: true

# Payment gateways config
payments:
  paypal:
    host: https://api.sandbox.paypal.com
    webhook-id: 2P781688HP282732G # sandbox
    client-id: AWJQJnM0O2nDEUgmMe9827bk73hjJdo3f4tPK9vwKvBVwFnDk1UGzk_Y2yeh5huiStwwdJVRmdOYWmhv
    client-secret: 1JE4291016473214C # this is fake, get the actual one from the PayPal dashboard
    connection-timeout: 10
    read-timeout: 30
